#include "main.h"

/**
 * _abs - computes and prints the absolute value of a number
 *
 * @n: integer input number
 *
 * Return: absolute value
 */

int _abs(int n)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
	_putchar('\n');
}
