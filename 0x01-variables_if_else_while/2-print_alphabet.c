#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/* betty style doc for function main goes there */
int main(void)
{
	char ch = a;

	for (char = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	return (0);
}
