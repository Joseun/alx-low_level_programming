#include "main.h"

/**
 * sub - prints the substraction of the number as arguments
 *
 * @a: number to be in operation
 * @b:  number to be in operation
 *
 * Return: 1 if error, result if success
 */

int sub(int a, int b)
{
	int sum = 0;

	sum = a - b;

	return (sum);
}
