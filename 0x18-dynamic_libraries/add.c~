#include "main.h"

/**
 * add - prints the addition of the number as arguments
 *
 * @a: number to be added
 * @b:  number to be added
 *
 * Return: 1 if error, sum if success
 */

int add(int a, int b)
{
	int sum = 0;

	sum = a + b;

	return (sum);
}
