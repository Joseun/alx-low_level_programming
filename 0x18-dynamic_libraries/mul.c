#include "op.h"

/**
 * mul - prints the multiplication of the number as arguments
 *
 * @a: number to be multiplied
 * @b:  number to be multiplied
 *
 * Return: 1 if error, multiplication if success
 */

int mul(int a, int b)
{
	int sum = 0;

	sum = a * b;

	return (sum);
}
